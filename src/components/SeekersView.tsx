import React from 'react';

export const SeekersView: React.FC = () => {
  return (
    <div
      id="seekers-view"
      className="w-full flex-1 min-h-[calc(100vh-8rem)] bg-white"
      aria-label="Empty Seekers View"
    >
      {/* Intentionally empty white canvas */}
    </div>
  );
};
